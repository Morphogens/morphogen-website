{
  "name": "morphogen-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild src/index.js --servedir=. --outdir=www/ --bundle",
    "build": "esbuild src/index.js --minify --bundle > www/index.js",
    "__start": "snowpack dev",
    "__build": "snowpack build"
  },
  "author": "Joel Simon",
  "license": "ISC",
  "dependencies": {
    "dat.gui": "^0.7.9",
    "regl": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.9.2"
  }
}
